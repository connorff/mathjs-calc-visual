export const PROJECT_NAME = "Motion Visualizer";
export const GITHUB_REPO = "https://github.com/connorff/mathjs-calc-visual";

export const ICON_WIDTH = 30;

export const INDEPENDENT_VAR = 't';

export const GRAPH_HEIGHT = 250;

export type AxisRange = [number, number];
export const DEFAULT_RANGE: AxisRange = [0, 10];
export const STEPS_PER_VIEW = 1000;
export const STEP_DEC_PLACES = 3;

export type VisualizerOptions = "values" | "velocity" | "acceleration";
